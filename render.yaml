services:
  - type: web
    name: concilia-erp
    runtime: docker
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
    disk:
      name: erp-db
      mountPath: /app/data
      sizeGB: 1
